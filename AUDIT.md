# 監査レポート — しりとりファイター

GIGA Standard v5 / Part III（改修モード `/rollout`）の記録です。
**測っていないものは「未計測」と書いています。「未計測」は ✅ ではありません。**

- 監査日：2026-08-09
- 改修前：`21c555f` ／ 改修後：このブランチ（`claude/rollout-76y60z`）
- 型：**A型（単一HTML完結）** … 改修前は GAS 配信（`code.gs`）も兼ねていましたが、
  CSP を入れるための外部ファイル化と両立しないため、**GAS 配信は廃止しました**（利用者判断）。
  `code.gs` は `index.html` を配るだけでデータを扱っておらず、失われた機能はありません。

## 測りかた（推測ではなく実測）

| 何を | どうやって |
|---|---|
| コントラスト | 実ブラウザで開き、**文字だけを透明にした画面を撮って、文字が乗る実画素を読む**。背景がグラデーション＋`backdrop-filter` なので `getComputedStyle` では測れない |
| | 320 / 375 / 1366 / 1920px の4幅 × 設定・バトル・エラー文・履歴・結果・提示モードの6場面、**下端より下はスクロールして**走査（改修後は延べ727か所） |
| | 固定オーバーレイの**裏に隠れた文字は除外**（暗幕の上の文字を測ると全部でたらめになる） |
| タップ領域 | `::after` の当たり判定込みで実測 |
| PWA | 実ブラウザで登録・初回リロード・更新・他アプリのキャッシュ・圏外起動を実行 |
| キーボード | Tab を送って到達できる要素を数え、モーダルの閉じ込めと Esc を実行 |
| アイコン | PNG を画素単位で読み、透明率とセーフゾーン外の中身を数えた |

**測定環境は `cdn.jsdelivr.net` / `fonts.googleapis.com` へ出られません。**
これは欠点ではなく、**学校のフィルタリングと同じ状態**です。実際に Google Fonts は
`ERR_CONNECTION_RESET` で落ちており、**端末側フォントに落ちた状態で上の数字を測っています**。

---

## A. 法務・配布

| # | 項目 | 前 | 後 | 実測 |
|---|---|:--:|:--:|---|
| A1 | LICENSE 実ファイル | ❌ | ✅ | MIT / Copyright (c) 2026 GIGAyama |
| A2 | .gitignore | ❌ | ✅ | `node_modules/` `dist/` `.clasp.json` `.env` `.DS_Store` |
| A3 | dependabot.yml | ❌ | ✅ | github-actions を monthly |
| A4 | README / MANUAL / AUDIT | △ | ✅ | 3点とも実態に合わせて更新 |
| A5 | CI（pull_request でも動く） | ❌ | ✅ | `.github/workflows/ci.yml` が push と pull_request の両方で `npm run check` と `npm test` |

## B. セキュリティ

| # | 項目 | 前 | 後 | 実測 |
|---|---|:--:|:--:|---|
| B1 | CSP（**入れたうえで動作確認済み**） | ❌ | ✅ | 下記 |
| B2 | 秘密情報・IDの直書きなし | ✅ | ✅ | `.clasp.json` / `.env` なし。APIキー・シートIDの直書きなし |
| B3 | OAuthスコープ最小 | ✅ | — | GAS 廃止により対象なし |
| B4 | postMessage の宛先が `*` でない | ✅ | ✅ | 使うのは Service Worker への `SKIP_WAITING` のみ |
| B5 | サーバー側5段ガード | — | — | 個人情報を扱わない。名前は端末内のみ・送信なし |
| B6 | CDN から取る実行コードが 0 | ✅ | ✅ | **0バイト**。外部は Google Fonts の CSS のみ（見た目だけ＝止まっても動く） |
| B7 | 外部資産の SRI と版の固定 | — | — | 実行コードの外部読み込みが無いため対象なし |

**CSP は入れたうえで動かして確かめました。**

- 本体の JavaScript（約980行）と CSS（約550行）を `index.html` から
  `js/app.js` / `js/kana.js` / `css/style.css` へ切り出し、`script-src 'self'` を通しました。
  `'unsafe-inline'` は足していません。
- `frame-ancestors` は `<meta>` では無視されるので書いていません。理由を `index.html` のコメントに残しています。
- インラインの `style` 属性1件（`style="margin-top:4px"`）はクラスに移しました。
- **動作確認**：設定→バトル→差し戻し→攻撃→提示モード→音切替まで実際に操作し、
  **CSP違反 0件・JSエラー 0件**（唯一のコンソールエラーは Google Fonts の遮断＝学校と同じ状態）。
- 計測ツールがスタイルを注入しようとして CSP に弾かれたことも、CSP が効いている証拠として確認しています。

## C. 堅牢性

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| C1 | LockService + try/finally | — | 書き込み処理なし |
| C2 | 自動復旧 | — | 対象なし |
| C3 | pagehide で記録確定 | — | 保存する記録が音のON/OFFのみ（変更時に即保存） |
| C4 | 通信失敗時のリトライと明示 | ✅ | 通信そのものが無い。Google Fonts が落ちても動作を確認 |
| C5 | localStorage.clear() を使っていない | ✅ | 使用キーは `sf-muted` の1つだけ |

## D. 表示（実ブラウザで測定）

| # | 項目 | 前 | 後 | 実測 |
|---|---|:--:|:--:|---|
| D1 | viewport に viewport-fit=cover | ✅ | ✅ | `index.html` / `offline.html` とも |
| D2 | 100dvh を使用 | ✅ | ✅ | `100vh` の直後に `100dvh` を重ねる形 |
| D3 | safe-area-inset を適用 | ✅ | ✅ | `.app` の上下左右＋更新トースト＋`offline.html` |
| D4 | clamp() による fluid type | ✅ | ✅ | 見出し・お題・ボタン |
| D5 | Canvas に devicePixelRatio 補正（上限2） | ✅ | ✅ | `Math.min(2, dpr)` + `setTransform`。提示モード切替時も測り直す |
| D6 | 320px 幅で横スクロールが出ない | ✅ | ✅ | **320 / 375 / 1366 / 1920px × 通常・提示モードの8通りすべてで 0件** |
| D7 | 画像に width/height、150KB以下 | ❌ | ✅ | アプリ本体に `<img>` なし。`docs/*.png` は 243.7→58.8KB / 237.0→67.5KB |
| D8 | **コントラスト 4.5:1 以上** | ❌ | ✅ | **5種（延べ27か所）→ 0件**（延べ727か所を走査） |
| D9 | **タップ領域 44px 以上** | ❌ | ✅ | **6種 → 0件** |
| D10 | prefers-reduced-motion（.01ms） | ✅ | ✅ | `0` にしていないので `fill-mode: forwards` が壊れない |
| D11 | forced-colors 対応 | ❌ | ✅ | ボタン・パネルの輪郭、選択状態の Highlight |
| D12 | 提示モード | ❌ | ✅ | 🔍 で1.5倍、⛶ で全画面。1600px 以上ではお題をさらに拡大 |
| D13 | 印刷CSS | — | — | 印刷する画面が無い |
| D14 | 拡大を禁止していない | ✅ | ✅ | `user-scalable=no` / `maximum-scale` なし |

### D8 コントラスト：直した5種（前 → 後）

| 文字 | 場所 | 直し方 | 前 | 後 |
|---|---|---|---:|---:|
| 「こうげき！」 | `#submitBtn` | 面の明るさは残し、**文字を濃く**（白 → `#3d2600`） | **1.96** | 8.4 |
| 「バトルスタート！」 | `#start-game-btn` | 同上 | **2.01** | 8.4 |
| 「P1」「P2」 | `.player-label` | 白抜きなので**面を濃く**（`#ff5e7d`→`#be123c` / `#38bdf8`→`#0369a1`） | **2.14** / 2.94 | 6.28 / 5.93 |
| 「もういちど！」 | `#rematch-btn` | 面を濃く（`#34d399`→`#047857`） | **2.41** | 5.5 |
| 「せっていにもどる」 | `#home-btn` | 面を濃く（`#818cf8`→`#4f46e5`） | **3.38** | 6.3 |

**色相は変えていません。** 面か文字のどちらかを1〜2段濃くしただけです。

あわせて、案内文にあたる**プレースホルダ**（`なまえを入力してね`）も
`rgba(255,255,255,.35)` から `#cfcbe8` にしました。装飾ではなく本文として測るためです。

お題の文字 `#next-char-disc` は **3.64〜4.11** ですが、24px を超える大きな文字のため要求は 3.0 で、基準を満たしています。

タイトル `h1` はグラデーションの塗り（`-webkit-text-fill-color: transparent`）のため
**自動計測の対象外＝未計測**です。数字は出していません。

### D9 タップ領域：直した6種

疑似要素で**当たり判定だけ**を広げました（ボタン自体を大きくすると詰めて組んだ行が折り返して別の破綻を生むため）。

| 要素 | 前 | 後 |
|---|---|---|
| 🔊 サウンド・🔍 提示・⛶ 全画面・📲 インストール | 40 × 40 | **44 × 44**（見た目は40のまま） |
| HP「30 / 50 / 100」・時間「10 / 20 / 30」・強さ3つ | 高さ **40** | 高さ **44** |

### ふりがな（`rt`）

改修前は `.message-box ruby rt` の1か所だけ色を決め打ちしており、そこは暗い面なので実測は基準内でした。
ただし**色のついた面に置いた瞬間に破綻する形**なので、まとめて継がせる指定を入れました。

```css
rt { color: var(--ink-dim); }
button rt, a rt, label rt, .mode-tab rt, .seg-btn rt,
[class*="btn"] rt, [class*="bg-"] rt { color: inherit; }
```

1か所ずつ潰すと必ず漏れます（他リポジトリでは、タグのラベルだけ直して送信ボタンが漏れていました）。

## E. PWA（実ブラウザで挙動を測定）

| # | 項目 | 前 | 後 | 実測 |
|---|---|:--:|:--:|---|
| E1 | manifest の id/scope/start_url が絶対パス | ❌ | ✅ | 3つとも `/Shiritori_fighter/`。`id` は改修前は**未設定** |
| E2 | アイコン4種 + 透明を含まない apple-touch-icon | ✅ | ✅ | `apple-touch-icon.png` は**完全透明 0.0% / 半透明 0.0%** |
| E3 | beforeinstallprompt を head 最上部の外部ファイルで捕捉 | ❌ | ✅ | `install-hook.js` を `<head>` の最初のスクリプトとして同期読み込み |
| E4 | インストールボタン（案内できるときだけ） | ✅ | ✅ | 合図が来たときだけ表示。インストール済み（standalone）では出さない |
| E5 | sw.js が自アプリ接頭辞のキャッシュのみ削除 | ✅ | ✅ | **実測**：`other-app-static-v1` と `keisan-card-static-v1` を置いて版を上げ、**両方残った** |
| E6 | sw.js が localStorage に触れていない | ✅ | ✅ | 記述なし |
| E7 | 更新通知（押すまで切り替わらない） | ❌ | ✅ | **実測**：版を上げて3秒放置 → `waiting=true`（前は `false`＝勝手に切り替わっていた）。「さいしんに する」を押すと**読み込み直し1回**で切り替わり、古いキャッシュが消えることも確認 |
| E8 | 初回訪問で勝手にリロードしない | ✅ | ✅ | **実測**：まっさらな状態で1回開き、画面遷移 **1回** |
| E9 | Service Worker が実際に登録されている | ✅ | ✅ | **実測**：`getRegistration()` が `active=true` を返す。登録は `readyState` の分岐つき |
| E10 | offline.html | ❌ | ✅ | **実測**：本体のキャッシュだけ消して圏外にすると `つながっていません` が出た |
| E11 | APP_VERSION を今回のリリース値に更新 | △ | ✅ | `v1.0.1` → **`v1.1.0`** |
| E12 | maskable のセーフゾーン外の中身 0.2% 以下 | ❌ | ✅ | **0.73% → 0.00%**（下記） |
| E13 | iOS の「ホーム画面に追加」手順を MANUAL に記載 | ✅ | ✅ | 記載あり |

**圏外の測り方について。** `setOffline(true)` だけでは足りませんでした。
**Playwright の `setOffline` は Service Worker 自身の `fetch()` を止めません。**
そのまま測ると「圏外のはずなのに本物のサーバから返ってきたもの」を見ることになります。
サーバを実際に止めて測り直したところ、`offline.html` が出ることを確認できました。

### E12 maskable アイコン

| | 前 | 後 |
|---|---:|---:|
| セーフゾーン外の中身 | **0.73%**（全画素比） | **0.00%** |
| いちばん遠い中身 | 安全圏の **123%** | 安全圏の内側 |

症状は「欠け」ではなく「はみ出し」でした（稲妻の上部が円の外）。
中身を 0.878 倍にして中央へ寄せ、**下地はいちばん外側の画素を外へ引き伸ばして端まで届かせて**います。
単色のグラデーションを敷き直すと角丸四角の輪郭が影として残るため、元の絵の下地をそのまま伸ばしました。

## F. アクセシビリティ・性能

| # | 項目 | 前 | 後 | 実測 |
|---|---|:--:|:--:|---|
| F1 | alt / aria-label / aria-live / role="alert" | △ | ✅ | 差し戻しの文言を `role="alert"` の入れ物にも入れるようにした |
| F2 | モーダルに role="dialog"・Esc で閉じる | ❌ | ✅ | **実測**：表示時にフォーカスが中へ移り、Tab を6回送っても外へ出ず、Esc で閉じて設定画面に戻る |
| F3 | キーボードのみで全機能に到達 | △ | ✅ | **実測**：Tab で 13種すべてに到達（開始ボタン・入力欄・こうげきボタンを含む） |
| F4 | rt の色を決め打ちしていない | △ | ✅ | 色のついた面では継がせる指定を追加 |
| F5 | 初回JS 300KB以下 | ✅ | ✅ | JS 約35KB / CSS 約17KB / 総アセット 約120KB |
| F6 | 1ファイル 5,000行 / 400KB 以内 | ✅ | ✅ | 最大は `js/app.js` 約1,000行 |

## G. 学習ログ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| G1 | study.v1 準拠 | — | 記録機能そのものが無い（対戦結果を残さない設計） |
| G2 | 中断記録・5分ルール | — | 同上 |

> 学習ログ（`study.v1`）を入れるかどうかは、**まだ決めていません。**
> 入れるなら「1試合の語数・時間・使ったことばの長さ」あたりが候補ですが、
> 名前を持たない設計を崩さないことが前提になります。判断が要る項目として残します。

## P2. 画像（前 → 後）

| ファイル | 前 | 後 |
|---|---:|---:|
| `icons/icon-192.png` | 22.7 KB | **7.9 KB** |
| `icons/icon-512.png` | 84.6 KB | **31.6 KB** |
| `icons/icon-maskable-192.png` | 18.5 KB | **6.1 KB** |
| `icons/icon-maskable-512.png` | 70.7 KB | **28.1 KB** |
| `icons/apple-touch-icon.png` | 16.5 KB | **6.6 KB** |
| **アイコン合計** | **213.0 KB** | **80.3 KB** |
| `docs/screenshot-setup.png` | 237.0 KB | **67.5 KB** |
| `docs/screenshot-battle.png` | 243.7 KB | **58.8 KB** |

すべて 256色パレット PNG です。**いちばん軽い版ではなく、上限に収まる中でいちばん色数の多い版**を選んでいます
（下地がグラデーションなので、色数を落としすぎると縞が出るため）。
角丸の透明は保たれており、`apple-touch-icon` は透明 0.0% のままです。
スクリーンショットは配色を変えたため**撮り直し**ました。

## P4. 品質ゲート

`npm run check`（CI と同じもの）が通ることを確認しています（error 0件 / warn 0件）。

**ゲートは「わざと壊して落ちること」を確かめてから信用してください。**
`npm test` が、複製をわざと壊して**28種の検査が1件ずつ落ちること**を自動で確かめます。
この確認をしたことで、**検査そのものの不具合が3件**見つかりました。

| 見つかった不具合 | 中身 |
|---|---|
| `INLINE_SCRIPT` の誤検知 | 「インラインの `<script>` を書き足すな」という**注意書き自体**に反応していた。判定前にコメントを落として解決 |
| `FORCED_COLORS` の取りこぼし | 本体の CSS から消しても、`offline.html` の分で通ってしまった。本体のスタイルだけを見るように修正 |
| `SW_CONTROLLERCHANGE` の甘さ | 変数名（`asked` を含むか）で見ていたため、名前を変えるだけですり抜けた。**「その変数が SKIP_WAITING を送る場所で true になるか」**を見るように修正 |

---

## 測っていないもの（重要）

- **本番 `gigayama.github.io` での確認**：作業環境からプロキシ 403 で到達できない
- **実機**：Chromebook・iPad・電子黒板・スマホ実機での表示は未計測（測定は Chromium のエミュレーション）。
  **提示モードが「教室のいちばん後ろの席から読めるか」は、実際の電子黒板で確かめてください**
- **iOS Safari でのホーム画面追加とアイコンの見え方**：未計測（画像に透明が無いことは実測）
- **Android の丸い切り抜きでの maskable の見え方**：未計測（セーフゾーンの画素は実測）
- **LCP / CLS の実測値**：未計測
- **Google Fonts が届く環境での表示**：未計測（本監査は遮断された状態＝学校と同じ条件で測定）
- **音声入力（Web Speech API）の動作**：測定環境に無いため未計測。マイクボタンのタップ領域も未計測
- **タイトル `h1` のコントラスト**：グラデーション塗りのため自動計測の対象外

## 人間に確認してほしいこと

1. **GAS 配信の廃止**（決定済み・実施済み）。`code.gs` を削除しました。
   もし GAS で配っている先生がいる場合は、GitHub Pages の URL に切り替えてもらう必要があります。
2. **公開パスが `/Shiritori_fighter/` に固定**されました。別名のリポジトリへコピーして使う場合は、
   `manifest.webmanifest` の3つと `index.html` の読み込みパスを**最初に**書き換えてください。
3. **`file://` で直接開けなくなりました**（絶対パスのため）。手元で見るときはローカルサーバが要ります（README に手順あり）。
4. **学習ログ（`study.v1`）を入れるかどうか**は未決です。
5. **`scripts/lib/project-quality.mjs` は、この改修で新しく書いたものです。**
   GIGA 全体の「正本」がある場合は、**バイト単位でそちらに差し替えてください。**
   Part I 固有の検査は `scripts/lib/giga-v4-checks.mjs` に分けてあるので、差し替えても壊れません。
