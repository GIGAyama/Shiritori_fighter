# ロールアウト記録 — Shiritori_fighter

GIGA Standard v5 / Part III（`/rollout`）の作業記録です。
**他のリポジトリにも効く知見**を中心に残します。

- 実施日：2026-08-09
- 前：`21c555f` ／ 後：`claude/rollout-76y60z`
- 実測の詳細は [AUDIT.md](AUDIT.md)

## やったこと

| フェーズ | 内容 |
|---|---|
| P0 | LICENSE / .gitignore / dependabot.yml / CI（pull_request でも動く） |
| P0.5 | 対象なし（CDN から取る実行コードは元から 0バイト） |
| P1 | コントラスト5種 → 0件、タップ領域6種 → 0件、`forced-colors`、`rt` の継承、モーダルの Esc とフォーカス、`role="alert"`、PWA 一式（manifest 絶対パス・`install-hook.js`・更新通知・`offline.html`・SW 書き換え）、CSP（外部ファイル化を伴う） |
| P2 | アイコン 213.0→80.3KB、スクリーンショット 480.7→126.3KB、maskable の作り直し |
| P3 | 提示モード（🔍 / ⛶）、README・MANUAL・AUDIT の更新 |
| P4 | 品質ゲート（`npm run check`）＋**わざと壊す自己テスト28種**（`npm test`） |

## 他のリポジトリにも効く知見

### 1. `setOffline` では圏外を測れない

**Playwright の `context.setOffline(true)` は、Service Worker 自身の `fetch()` を止めません。**
そのまま測ると「圏外のはずなのに本物のサーバから返ってきたもの」を見ることになり、
`offline.html` に落ちるかどうかを確かめられません（本件では、本体キャッシュを消したのに
アプリが表示され続けて 30分ほど原因を探しました）。

**サーバを実際に止めて測ってください。**

```javascript
await new Promise((r) => { server.close(r); sockets.forEach((s) => s.destroy()); });
```

`keep-alive` が残るので、`server.close()` だけでは足りません。開いているソケットも壊すこと。

### 2. コントラスト計測は「隠れている文字」を必ず除外する

結果画面のような固定オーバーレイがあるアプリでは、**その裏の文字まで測ると全部でたらめ**になります。
本件では最初の測定で 12件の基準未満が出ましたが、そのうち **10件は暗幕の裏にある文字**でした。

`document.elementFromPoint` で重なり順を見て除外します。ただし注意が2つあります。

- **一律に `pointer-events: auto` を当ててはいけない。**
  全面を覆う `pointer-events: none` の演出キャンバス（`#fx-canvas` など）がある場合、
  すべての要素が「隠れている」と判定されて、測定対象がほぼ全滅します。
  **測る要素自身が `none` のときだけ**、その連鎖を一時的に戻して判定すること。
- **ビューポートの外は測れない。** スクロールして走査しないと、
  設定画面の下にある「開始ボタン」のような**いちばん大事な要素を丸ごと取りこぼします**（本件で実際に取りこぼした）。

### 3. CSP を入れると計測ツール自身が弾かれる

`style-src 'self'` を入れると、Playwright の `addStyleTag` が拒否されます。
これは CSP が効いている証拠なので、**計測用のコンテキストだけ `bypassCSP: true`** にしてください。
リポジトリ側に `'unsafe-inline'` を足してはいけません。
**CSP 違反そのものの確認は、迂回していない別の実行（実操作の通し）で行うこと。**

### 4. 検査は必ず「わざと壊して」確かめる

自己テストを書いた結果、**検査そのものの不具合が3件**見つかりました。

| 不具合 | 中身 |
|---|---|
| `INLINE_SCRIPT` の誤検知 | 「インラインの `<script>` を書き足すな」という**注意書き**に反応した。§P4 の `SW_LOCALSTORAGE` とまったく同じ形。**判定の前にコメントを落とす**のは共通の作法にすべき |
| `FORCED_COLORS` の取りこぼし | 全 CSS・全 HTML を連結して見ていたため、本体から消しても `offline.html` の分で通った。**本体のスタイルだけを見る** |
| `SW_CONTROLLERCHANGE` の甘さ | 変数名に `asked` が含まれるかで見ていたので、名前を変えるだけですり抜けた。**「その変数が `SKIP_WAITING` を送る場所で true になるか」**まで見る |

**「0件でした」だけでは、検査が動いているのか何も見ていないのか区別できません。**

### 5. 明るい面の白抜き文字は、面ではなく文字を濃くする手もある

黄橙（`#fbbf24`）の上の白文字は **1.96**。面を濃くすると印象が変わってしまう場合、
**文字を濃く**する（`#3d2600` で 8.4）ほうが元の見た目に近いまま基準を満たせます。
本件ではすでに「インストール」ボタンが同じ手（`#442c00`）を使っていたので、
アプリ内の作法とも一致しました。**面を濃くするか文字を濃くするかは、周りの作りを見て決める。**

### 6. maskable は「欠け」より「はみ出し」を疑い、下地は引き伸ばす

`purpose: "maskable"` と書いてあっても中身が maskable とは限りません。
本件は**下地は端まであるのに、絵が安全圏の 123% まではみ出している**形でした。

直し方：**元の絵をそのまま縮小して中央へ寄せ、はみ出した外周は最外周の画素を引き伸ばして埋める。**
単色グラデーションを敷き直すと角丸四角の輪郭が影として残ります。
コード上は「サンプリング座標を画像範囲にクランプする」だけで実現できます。

また、中身を数えるときの色分類には注意が要ります。本件では濃紺の縁取りを拾おうとして
**下地の暗い紫まで「中身」と誤判定し、はみ出しを 3.00% と過大に見積もりました**（正しくは 0.73%）。
**下地と中身は、色で区別できることを必ず確かめてから数える。**

### 7. パレット化は「いちばん軽い版」を選ばない

色数を落とすほど軽くなりますが、**下地がグラデーションだと縞が出ます。**
上限（512は60KB、それ以外は30KB）**に収まる中でいちばん色数の多い版**を選ぶこと。
本件はすべて 256色のまま、213.0KB → 80.3KB になりました。

### 8. GAS 兼用リポジトリでは、CSP と外部ファイル化が両立しない

Apps Script は `.gs` と `.html` しか置けないため、`js/app.js` を参照した瞬間に GAS 側で 404 になります。
**「GAS 配信をやめる」か「`index.html` を原本に `css.html` / `js.html` を作るビルドを足す」かの二択**です。
本件は前者（GAS 側はデータを扱っておらず、`index.html` を配るだけだった）。

**同じ構成のリポジトリを見つけたら、まず `code.gs` が何をしているかを読むこと。**
データを扱っていないなら、廃止して1本にするほうが保守が軽くなります。

## 同じ形が他にないかを数えるための1行

```bash
# ブラウザ内 Babel / Tailwind CDN
grep -rln "babel/standalone\|cdn.tailwindcss.com" $(git ls-files '*.html')
# ふりがなの色の決め打ち（inherit の受けが無いもの）
grep -rn "^\s*rt\s*{" $(git ls-files '*.html' '*.css')
# install の中の skipWaiting
grep -l "skipWaiting" $(git ls-files '*sw.js')
# 他アプリのキャッシュ全削除（startsWith で絞っていないもの）
grep -Ln "startsWith" $(git ls-files '*sw.js')
# manifest の相対パス
grep -n '"start_url": "\."' $(git ls-files '*.webmanifest')
# 明るい面に白抜き文字（実測が要るが、当たりを付ける用）
grep -rn "color: *#fff" $(git ls-files '*.css' '*.html') | grep -i "gradient" 
```

## この先の宿題

- 実機（Chromebook・iPad・電子黒板）での確認 … 作業環境からは本番へ到達できないため未実施
- 学習ログ `study.v1` を入れるかどうかの判断
- `scripts/lib/project-quality.mjs` を GIGA 共通の正本へ差し替え
